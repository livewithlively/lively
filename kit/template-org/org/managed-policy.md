<!--
  Managed Policy — 강제(오버라이드 불가)로 *의도된* 규칙 레이어.
  ──────────────────────────────────────────────────────────────────────
   · 합성 시 항상 컨텍스트 최상단(최우선)으로 로드되므로 **짧고 절대적인 규칙만** 두세요(--check 가 분량을 검사).
   · 실제 '오버라이드 불가' 강제력을 원하면(규제 등): 이 내용을 Claude managed-settings 의 "claudeMd" /
     Codex requirements.toml 로 배포하세요(어댑터 managed 예시 참고). 그냥 두면 권장 규칙으로 합성됩니다.
   · 보안 통제는 여기 텍스트가 아니라 게이트웨이(RLS·스코프·감사)·OS/MDM 에 있습니다 — 여긴 작업 품질 가이드.
   · 선택 파일: 없어도 생성기는 빌드됩니다(이 레이어를 스킵). 팀 강제 규칙이 없으면 파일째 지워도 됩니다.
-->

# <회사명> — 강제 규칙 (Managed Policy)

이 규칙들은 어떤 개인/프로젝트 설정으로도 덮을 수 없다(고 의도한다).

1. **<규칙 1 — 예: 근거·출처 필수>** — <한 줄 설명>.
2. **<규칙 2 — 예: 시크릿 보호>** — 자격증명·토큰·키를 커밋하거나 로그/출력에 노출하지 않는다.
3. **<규칙 3 — 예: 되돌리기 어렵거나 외부로 나가는 작업은 사전 확인>** — 삭제·덮어쓰기·외부 전송은 명시적 승인 없이는 하지 않는다.
4. **<규칙 4 — 예: 사실대로 보고>** — 실패는 실패로, 건너뛴 건 건너뛰었다고 말한다.

<!-- 규칙은 5~7개 이내 권장. 길어지면 권장 사항은 org/org-defaults.md 로 옮기세요. -->
