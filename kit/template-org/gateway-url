# 사내 MCP 게이트웨이 주소(예: http://gw.example.com:8080/mcp 또는 http://localhost:8080).
# 이 줄(#)은 주석 — setup 이 읽을 때 무시됩니다. 아래에 실제 URL 한 줄만 두세요.
# 비워 두면(또는 이 파일 삭제 시): 컨텍스트(파일)만 전달되고 DB/도메인 라이브 조회는 빠집니다.
# 토큰/시크릿은 절대 여기 두지 마세요 — URL 만. 토큰은 setup 시 멤버가 개별 입력(~/.lively/token).
