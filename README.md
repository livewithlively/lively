# CLA signatures

This branch stores contributor CLA signatures collected by the CLA bot
(`.github/workflows/cla.yml` on `main`). It intentionally contains no source code.

Do not delete or rewrite this branch — it is the record of who agreed to
[CLA.md](../../blob/main/CLA.md), and the bot appends to it on every first-time
contribution.
