// 리브 폴더(#4032) — 리브 세션이 열리는 자리(<개인 루트>/liv)이자 리브 부팅 훅의 게이트(basename(cwd) === "liv")다.
//  라이블리가 정한 자리라서, 서버가 이 폴더에 연 리브 세션(task 종류)은 세션 생성의 신뢰 판정
//  (terminal/session-create-guards.ts)이 신뢰 대화상자를 대신 수락한다. 사람이 같은 이름의 폴더를 골라 연 세션은 사람이 답한다.
//  가져오기가 없는 잎 모듈이다 — terminal/ 의 순수 판정이 org/liv/session.ts(로거·세션 관문)를 끌어오지 않게 여기 따로 둔다.

/** 리브 세션의 작업 폴더(개인 루트 아래). 리브 부팅 훅의 게이트가 이 이름을 본다. */
export const LIV_SUBPATH = "liv";
