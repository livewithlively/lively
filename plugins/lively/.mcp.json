{
  "_comment": "userConfig 값은 url·headers 에 ${user_config.KEY} 로 치환된다(셸을 타지 않는 필드라 허용). 셸형 훅 커맨드·headersHelper 에는 못 쓰며, 훅에는 CLAUDE_PLUGIN_OPTION_<KEY> 환경변수로 같은 값이 전달된다 — session-preload.mjs 가 그 폴백을 읽는다.",
  "mcpServers": {
    "lively": {
      "type": "http",
      "url": "${user_config.gateway_url}/mcp",
      "headers": {
        "Authorization": "Bearer ${user_config.token}"
      }
    }
  }
}
