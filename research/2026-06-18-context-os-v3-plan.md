# Context OS v3 — 실행 계획 (캐노니컬)

> 출처: 발견+설계 워크플로우 `wf9fwhmow`(9 에이전트, 발견 A~E + 설계 1~3 + 완전성 비판). 본 문서가 v3 빌드의 **단일 ground-truth 계획**이다. 빌드 에이전트는 이 문서 + 해당 소스를 읽고 구현한다. 결정은 윤상민 위임("설계 나오면 확인받지 말고 판단해서 끝까지").

## 0. 운영 규칙 (밤샘 자율)
- **메인 체크아웃 직접 작업 + :8080 직접 반영**(워크트리 불요 — 동시세션·런칭 없음). 단 **페이즈 순차 실행**(메인 충돌 방지), 각 페이즈: 구현 → 적대검증 → gates(build/typecheck/test/parity) → **커밋** → 배포(restart 또는 정적) → e2e 테스트.
- **품질 바 유지**: 비파괴(item→item_legacy 보존, DB 백업/롤백 경로), 게이트 통과 후에만 라이브, 결과 정직 기록("라이브 반영"은 실제 배선 확인 후).
- 시크릿 절대 출력 금지. 모든 변경 출처 명기.

## 1. 확정 아키텍처 결정
- **D-ARCH: knowledge_unit = 단일 캐노니컬 표면.** 커넥터가 활동/태스크를 `kind=A/W` 행으로 knowledge_unit에 적재(source/external_id/fields/raw/sync_state + 다중도메인 매핑 조인). `item`은 **item_legacy로 리네임·보존**(비파괴), 읽기경로 전환 후 아카이브. 물리 2계층이지만 *표면은 단일* — 사용자 의도(커넥터→신모델)와 데이터 강제(PK/다중매핑/상태축 상이)를 화해. **정직: "단일 표면 + 레거시 보존", 완전 단일테이블 아님.**
- **D-GT: ground-truth = `kind_registry`(description/criteria/storage/delivery 컬럼 채움) + 신 `data_source` 레지스트리(소스별 수집방식).** 런북(LLM)·웹(`#/learn`, 비개발자)이 **여기서 렌더**(non-stale). 하드코딩(RECALLED_KINDS 등) 제거→테이블.
- **D-INJ: 훅이 지식 인덱스(buildKnowledgeIndex/org_preview)를 실제 주입.** 현 훅은 status만 주입·인덱스 미배선. 정적 폴백(context.md에 인덱스 박기)도. **로컬 ~/.claude는 주입 검증 후** 포인터/스코프로 화해(순서 강제 — 안 그러면 메모리 공백).
- **D-G: G kind 정합화** — 원 분류 G=부채(debt)이나 registry G=Glossary/Graph. 결정: **부채는 domainmap debt_finding을 federate(파생 뷰)**, G kind는 라벨/정의를 ground-truth 3곳(스펙·런북·웹) 모두에 일관 명문화(중복정의 금지).
- **D-PROJ: project '붕뜸' = provenance_kind 분리.** `initiative`(PM-tool provenance, 4행) vs `code_grouping`(doc-derived, 45행·touch 558). W(clickup task)↔initiative 링크. code_grouping 재배선은 별 마이그(설계는 본 문서에 존재, 실행은 후속).
- **D-RENAME: domain_rename = soft-alias**(old_key→new_key 매핑 테이블, 물리 key 불변) — items DB와 domainmap DB는 **별 DB라 cross-DB 트랜잭션 불가**(H3). 물리 cascade 금지.
- **D-DISTILL: 증류 v1 = 규칙만**(LLM 0; 예: 같은 도메인 N회 declared task → D 후보), opt-in, **proposed TTL/캡 + 멱등 후보키(source_ref+kind)**로 적체·재후보화 억제(H4). LLM 증류는 v2.

## 2. 치명 완화 (각 페이즈에 반드시 내장)
- **🔴H1 시크릿 주입유출**: `fields`/`raw`(커넥터 원본) 추가 시 — (a) ingest가 knowledge_unit 쓰기 전 `redactDeep`, (b) `buildKnowledgeIndex`/preview 출력에 `assertNoHardSecrets` 게이트, (c) ctx_cat/memory_search **pull 경로도 redact**(인덱스 비주입만으론 불충분). publish.ts/materialize.ts/delivery.ts preview에 redact 0건이 현 사실.
- **🔴H2 confidence='observed' 파급**: 신값 추가 시 — review 큐 제외, overview 별도 카운트("수집물 N"), **주입 인덱스 영구 제외**를 불변식으로. app.js confidenceDot 3값 가정 갱신.
- **🔴H3 rename 원자성**: soft-alias(위 D-RENAME).
- **🔴H4 증류 골격**: 규칙판+TTL/캡(위 D-DISTILL).
- **🟡M-가 parity/표면**: org/ctx/materialize **테스트 파일 부재** → 신 툴 추가 전 테스트 선작성. 31→확장 시 `capabilities/index.ts` 카운트 주석 동기화 + 툴표면 단언 테스트 + 신툴 각 expose·parity 케이스.
- **🟡M-나 project 재배선**: code_grouping vs initiative UNIQUE(repo,key) 공간·네임스페이스 혼재 — 별 테이블 분리 또는 key kind-접두.
- **🟡M-다 고객 마이그**: 마이그 멱등·롤백(`--check`에 스키마버전·observed enum·source 컬럼 검사), INSERT…SELECT 1회성 가드.
- **🟡M-라 성능**: item 흡수로 knowledge_unit 급증 + ILIKE full-scan → **pg_trgm GIN 인덱스**(pgvector 없이 ILIKE 가속), A/W는 검색 source 필터.
- **🟡M-마 권한**: 신 CRUD(repo/domain/project) 각 op scope·protected repo·agent vs human을 화이트리스트(domainmap-curation.ts 패턴)로.
- **🟢L**: kinds[] CHECK 가드 실제 추가(17/43 실사용), 재발행 멱등(인덱스 중복 누적 방지), 레거시 read 래퍼 전환 순서(archive 전 전환), G≠debt 3곳 일관.

## 3. 실행 순서 (11단계 → 페이즈; 의존성 강제)
- **P-V3-1 주입 배선**(순서1·2): 훅이 지식 인덱스 주입 + 정적 폴백(context.md 인덱스). redact 게이트(H1-b). 최고가치·최저위험. 훅 소스=workflow-std/hooks/session-preload.mjs(생성물 ~/.lively/hooks/ 재발행). org_preview 엔드포인트 활용. 멱등 재발행 검증.
- **P-V3-2 ground-truth+런북+웹**(순서3·4): kind_registry 확장·시드(description/criteria/storage/delivery) + data_source 신설(비파괴 ADD COLUMN). 하드코딩→테이블. 런북 빌더(build-classify-runbook.mjs) + 웹 `#/learn`(12 kind 설명·분류기준·저장·전달 + 소스별 수집, 비개발자 쉬운말, 마크다운 렌더 재사용). non-stale 불변식 실증. G≠debt·kinds[] 명문화.
- **P-V3-3 단일스토어 데이터층**(순서5·6·7): knowledge_unit 스키마 확장(source/kind/sync_state/external_id/external_url/parent/fields/raw + observed enum) + 다중도메인 매핑 조인 + pg_trgm. 커넥터/pm_* 적재 전환(item→knowledge, kind A/W). 읽기경로 전환 + item→item_legacy. **H1·H2·M-다·M-라 내장. 테스트 선작성(M-가). 최대 수술·최대 위험 — 가장 엄격 검증·롤백(item 보존).**
- **P-V3-4 도메인/repo/project CRUD**(순서8): repo CRUD, domain CRUD(soft-alias rename), project provenance_kind 분리·W↔initiative. H3·M-나·M-마 내장.
- **P-V3-5 로컬 화해**(순서9): ~/.claude 중복 → 포인터/스코프. **P-V3-1 주입 검증 후에만.**
- **P-V3-6 증류 v1**(순서10): 규칙판+TTL/캡, opt-in.
- **P-V3-7 후속**(순서11): UserPromptSubmit recall + pg_trgm/pgvector. (시간/안정성 보고 판단.)

## 4. 페이즈 공통 산출
각 페이즈 완료 시: 커밋 해시·변경파일·게이트결과·적대검증 발견/수정·라이브 배포·e2e 결과·잔여리스크를 본 문서 말미 "진행 로그"에 추가(또는 보고). 모든 주장은 실측.

## 진행 로그
- **P-V3-1 주입 배선 ✅ (커밋 3b2b21f, :8080 라이브)** — 훅 소스는 이미 org_preview 인덱스 fetch하나 설치본(06-12) stale→재발행으로 배선 활성. redact 출력게이트(materialize.ts=정적/라이브/웹 단일소스 + publish.ts, 마스킹·fail-open, 합성시크릿 5종 [REDACTED] 실증). org 테스트 선작성(materialize.test 6). 검증: 블로커0, preview·훅 stdout 둘다 Knowledge Index 주입 실증, 멱등 byte-identical, test100·parity54·31툴 불변. 잔여(계획정합): H1-c pull redact→P-V3-3, 오프라인 context.md 인덱스→P-V3-5(진실원천 이원화 화해와 함께).
- **P-V3-2 ground-truth+런북+웹 ✅ (커밋 8c578ac, :8080 라이브)** — kind_registry +criteria/storage/delivery·12 kind 정의 + data_source(4소스·discord=dropped) + kinds[] CHECK(L-가) + 런북 빌더→knowledge-types.md(168줄) + 웹 #/learn(12카드+소스표·XSS실증) + 하드코딩 RECALLED_KINDS→registry 조회. G=Glossary/Graph 3곳 일관(L-라, debt=domainmap federate). non-stale 실증. /api/ui/learn=12kind·4소스 라이브. test·parity 55·31툴 불변. 블로커0.
- **P-V3-3 단일스토어 데이터층 = 3분할**(라이브 데이터 수술 위험 분산): 3a 스키마 확장(additive)·3b 커넥터 적재 전환·3c 읽기 전환+item_legacy. item 비파괴 보존 끝까지.
- **P-V3-3a 스키마 확장 ✅ (커밋 예정, DB 라이브 적용됨)** — knowledge_unit +12컬럼(source/external_system·instance·id·url/sync_state/occurred·last_synced/parent_*/fields/raw, 43행 source='authored' 백필) + 6인덱스(external 부분UNIQUE 멱등키·source·occurred·parent·title/body trgm GIN) + observed enum(H2: 인덱스 영구제외·overview observed_count 분리·검토큐 제외 실증) + 조인테이블 knowledge_unit_domain/project(다중귀속·cascade·CHECK) + pg_trgm v1.6. 멱등 3x·43행/12kind 무손상·DB통합테스트 7. parity 55·31툴 불변. 잔여(계획정합): H1-c pull redact·external_instance NULL 가드·app.js confidenceDot=P-V3-3b. (DB-스키마 --check CLI 부재 → schema-p3a.test 가 information_schema/pg_constraint 명시검사로 M-다 동치.)
- **P-V3-3b 마이그+redact+observed UI ✅ (커밋 예정, DB·UI 라이브)** — item 71→ku observed 71(notion41→A·clickup30→W), item 무손상, 다중도메인 154/프로젝트 70 보존, 멱등(재실행 0). H1 적재 redact + H1-c pull redact(mapKnowledge 단일 choke-point, getKnowledgeRaw로 쓰기경로 원본보존) 실증(합성 시크릿 [REDACTED]). observed 인덱스 영구제외(H2)·overview observed_count=71·검토큐 제외. app.js observed UI("수집물 N"·confidenceDot·browse 필터, XSS안전). ku=43 authored+71 observed. parity 55·31툴. 마이그 스크립트 migrate-items-to-ku.mjs(멱등·--dry).
- **P-V3-3c 데이터층 단일화(안전 서브셋) ✅ (커밋 예정, 스키마 라이브)** — 읽기경로 shape 어댑터 **거부**(정당: item.id BIGSERIAL vs ku TEXT PK, MCP 정수id 계약·parity 과위험). 적재 전환: knowledge-mirror.ts 단일 choke-point에서 ingestItems/declare/supersede가 ku observed A/W 듀얼라이트(best-effort·H1 redact·external 멱등키·clickup→W/notion→A, slack/discord skip). pm_* 무수정 자동미러(외부계약·audit 보존). 읽기(search_items/get_item/list_unmapped/curate/pm)=item 프리즈 유지·shape 0변경. item→item_legacy 비파괴 VIEW(물리 item 보존·롤백 DROP VIEW). parity 55·31툴·deep-equal 불변. 잔여: 미러는 커넥터run/restart 시 활성, 도메인매핑(curate) 미러갭=후속, dualwrite.test 라이브 audit 2행(테스트위생).
- **P-V3-4a 도메인/repo CRUD + 웹 통제어휘 + 권한 ✅ (커밋 안 함, DB·웹정적 라이브)** — 신 MCP 5종(31→36): domain_create·domain_rename·repo_create·repo_rename·repo_deprecate(capabilities/domainmap-crud.ts, MCP+REST co-exposed, domainmap-curation 패턴). **soft-alias H3 실증**: domain_rename 이 물리 domain.key 불변 + domain_alias(repo_id,old_key→new_key) 적재 + 표시명만 갱신, resolveDomainKey(domain-alias.ts, 사이클·체인 가드)가 읽기/검증에서 해소 — items DB↔domainmap DB cross-DB 트랜잭션 회피(테스트로 물리 key 불변·옛/새 슬러그 동일 도메인 해소 증명). repo CRUD: repo.state 축(active|deprecated, CHECK), 보호 리포(lively) 가드. **웹**: 자유텍스트 도메인입력 3곳(browse 필터·유닛편집·메모)→domain_list 드롭다운(통제어휘, graceful 폴백)+repo 셀렉터, 신 관리 화면 '도메인·레포'(repo/도메인 CRUD·rename soft-alias 명시). ctx_save 도메인 검증 **엄격+alias 해소+graceful**(canonical 저장). **권한 M-마**: 3층(코어 보호리포·agent→human-origin rename 403 / capability repo rename·deprecate agent 금지 / scope+bearer). 게이트: build·typecheck·test(crud.test 18: create/rename/alias/사이클/권한, 합성repo 비파괴 정리)·parity 65(36툴). 라이브 비파괴(repo 3·domain 31·alias 0·leftover 0). 잔여: (a) 신 POST CRUD 라우트는 서버 **restart 후 활성**(정적 웹은 즉시 라이브, restart 금지로 미반영 — 픽업 시 e2e 권장), (b) admin 토큰 부재로 CRUD UI 는 canEdit 강제 후 스크린샷 검증(렌더만, 쓰기 미발생), (c) project provenance_kind 분리(D-PROJ/M-나)는 본 프롬프트 범위 밖 — P-V3-4 후속.
- **P-V3-4b project 붕뜸 해결 ✅ (커밋 예정, DB·웹정적 라이브)** — domainmap.project +provenance_kind(initiative|code_grouping CHECK) ADD+백필: **initiative 5**(PM-provenance 4 + human-큐레이션 infra-cicd 1; 계획 4/45와 정직한 일탈 — 사람선언 이니셔티브는 doc-파생 아님)·**code_grouping 44**(external_id NULL AND origin=agent), 0 NULL·오분류0·멱등. W↔initiative: 4 initiative external_id = 30 W유닛 list_id 4개 byte-identical, ku_project 30 declared/confirmed가 권위 링크(cross-DB FK 불가→슬러그 약결합, 억지조인 안 함). 네임스페이스(M-나): UNIQUE 충돌0, reconcile.upsertProject 신규에 reserved PM접두(clickup-/jira-…) 거부 가드. project_list/REST/웹 provenance_kind 노출+붕뜸 설명(비개발자). kind 라벨축 무수정(과적재가 붕뜸 원인→provenance_kind로 해소). test(provenance-kind 6)·parity 65·36툴·project49/touch558 무손상. 잔여: project_list provenance_kind는 restart 후 픽업·W링크 카운트 합산 미표시(cross-DB 회피).
- **P-V3-6 증류 v1(규칙판) ✅ (커밋 안 함, DB 비파괴=실 증류 미실행·합성 테스트만)** — **LLM 0**(측정가능 규칙만). 신규 `src/org/distill.ts`(증류 프레임워크+규칙엔진+TTL+confirm) + `scripts/distill.mjs`(opt-in CLI: --run/--dry/--confirm/--list) + `src/org/distill.test.ts`(7케이스 DB통합). **프레임워크**: 후보=knowledge_unit confidence='ai'·lifecycle='active'·source_ref `distill:<rule>:<seed>`(멱등 후보키). 멱등=ON CONFLICT(name)+사람손길 보호(human/reject 후보 안 덮음). TTL=미검토(ai·active·distill) 후보가 N일(기본30) 노후+rules가 더는 안 냄(dead seed)이면 superseded — **expire 를 upsert 후에** 불러 살아있는 시드는 갱신돼 생존(ordering=보호, 회귀테스트 포함). 캡=1회 신규 최대 N(기본20, 초과분 cappedOut). **규칙 2종**(우리 71 observed 에 깨끗이 떨어지는 것만): (a) R-DOMAIN-WORKLOAD=도메인당 distinct observed W≥3 → 활성작업영역 D 후보, (b) R-PROJECT-CLUSTER=declared 프로젝트(clickup list, mapped_by='declared'·state='confirmed')당 distinct W≥3 → 작업클러스터 K 후보. **dry 실측(실 DB 미변경)**: 5후보(D 2: domain-cartography 5W·workflow-standardization 4W [둘 다 proposed 링크=약신호, evidence 정직표기]; K 3: clickup-...960 21W·...963 4W·...967 3W [declared=강신호]). 캡20 이내(cappedOut 0). **정직한 가능범위**: W↔domain 링크는 전부 proposed(추론)라 D 후보는 본질적으로 약한 ai 제안(검토 필수) — 그래서 confidence='ai'·인덱스 비주입이 정확. 의미 요약·중복 병합·진짜 인사이트는 규칙판 불가(LLM 증류=v2 명시). **검토 통합**: 후보가 검토큐(overview.review_pending=ai·active)에 뜨고, confirm(distill.confirmCandidate: ai→human+source_ref=NULL → 인덱스 진입)·reject(기존 ctx_set_lifecycle=rejected). **주입 격리(H2 정합)**: materialize.buildKnowledgeIndex 에 distill source_ref 제외 추가(미검토 후보 항상-주입 차단, '승인 시 인덱스 진입'); observed 원본은 query-only 유지. **opt-in**: CLI 사람 실행(Stop훅/cron=v2). 게이트: build·typecheck·test(distill.test 7: 규칙·멱등·캡·TTL살아있음/dead·보호·주입격리, 합성 repo 'distilltest-repo' 격리=실 ku 후보 0)·parity 65·36툴 불변(CLI 라 MCP 표면 무변화). **비파괴 실증**: 실 ku 114(43 authored+71 observed) 무변·distill 행 0(실 증류 미실행, --dry 만). 잔여(정직): (a) 실 라이브 후보 적재는 사람이 --run 해야 발생(미실행 — 비파괴 준수), (b) materialize 변경은 서버 restart 후 픽업(현 라이브 distill 0건이라 인덱스 byte-identical=무영향), (c) confirm/reject 의 웹 UI 버튼은 미추가(CLI+기존 set-lifecycle 로 충분, 웹 검토버튼=후속), (d) 규칙 2종은 v1 한계 — 신호 종류 확장·LLM 의미증류는 v2.
- **P-V3-5 로컬 화해 ✅ (커밋 안 함=메인 루프, repo 코드 게이트 통과·DB read-only·로컬메모리 백업 후 변경)** — **Part A 정적 폴백 DB 통일**: `publish.ts` 에 `materializeStaticContext()`(DB materialize→generator buildStaticContext in-process import) 추가 + `runPublish` 가 발행물에 `context.md`(DB-인덱스)도 함께 굽도록 + `user-install.mjs` 가 발행물 context.md 우선(없으면 AGENTS.md 파생 폴백) + 두 어댑터(claude/codex install.mjs)가 게이트웨이 가용 시 `/api/ui/org/preview`(DB Knowledge Index) fetch → ~/.lively/context.md, 다운/오프라인이면 파일기반 buildStaticContext 폴백(graceful). **실증**: 임시 발행물 context.md=7446B·`# Knowledge Index` 포함·stale `Canonical Memory Index` 0 + materializeStaticContext 와 byte-identical(=라이브 preview 7446B 와 동일) → **정적·라이브·웹·발행 단일소스(DB buildKnowledgeIndex)**, 재발행 byte-identical(멱등·인덱스 중복0). 격리 HOME 어댑터설치: 게이트웨이 up→DB인덱스(7446B), 토큰없음→파일기반 폴백(1952B, Canonical) 실증. **Part B 로컬 메모리 화해**: 백업 `memory.bak.20260618T042253`(20파일·MEMORY.md md5 동일). 로컬 19메모리 분류 — **DUP 17**(authored ku 이름 정확일치=훅 주입 중복: competitive-landscape-2026-06 등) **→ MEMORY.md 에서 `(file.md)` 링크 제거하고 "→ org ku 캐노니컬, 훅 주입" 포인터로 강등**(파일 보존), **로컬전용 2**(context-os-v2-shipped 빌드상태·work-approach-ultracode 작업방식, ku 없음) **→ 정식 인덱스 링크 유지**. **무손실 19/19**(17 ku∈ / 2 로컬인덱스∈, lost=0), **이중주입 제거**(17개 CC-네이티브 링크 0 → 훅 ku 단일 주입), MEMORY.md 프론트매터 없는 인덱스 규약 유지, diff vs 백업=MEMORY.md 만 변경(17 DUP 파일 byte-identical 보존). 게이트: typecheck·test(신규 static-context.test 3 + 전체)·parity 65·36툴 불변. 잔여: 발행 시 context.md 굽기는 generator in-process import(buildStaticContext 순수함수라 안전 — runPublish 의 subprocess 격리는 잘못된입력 process.exit 회피용인데 materialize 산출물이 필수파일 항상 보장); 실 ~/.lively/context.md 갱신은 다음 어댑터 재설치/재발행 시 픽업(현재 라이브 훅은 이미 preview=DB 인덱스 fetch — 폴백만 stale, 그것도 본 변경으로 다음 설치부터 DB).
- **✅✅ v3 코어 P-V3-1~6 전부 완료·커밋·:8080 배포·최종 종합검증 `ship` (2026-06-18 새벽 자율).** 실제 커밋: `3b2b21f`(P1 주입배선)·`8c578ac`(P2 ground-truth/런북/웹)·`beb275e`(P3a 스키마)·`f717bee`(P3b 마이그+redact)·`c465ec9`(P3c 단일화/item_legacy)·`8ac5d03`(P4a 도메인CRUD/통제어휘)·`b9addf0`(P4b project)·`0ff94ee`(P5 정적통일)+workflow-std `03a1aef`·`e503817`(P6 증류). **[정정: 위 개별 항목의 "커밋 예정/안 함"은 작성 시점 표기 — 전부 메인 루프가 커밋·배포함.]** `~/.lively/context.md` 도 라이브 preview 로 갱신(12166B·Knowledge Index 포함 → 오프라인 폴백 갭 closed). **최종검증(`w589bxce2`) 백엔드·웹 둘 다 `ship`·blocker/high/med 0**: 원우려 8/8·HIGH 완화 4/4(H1 redact 직접INSERT 우회도 pull 차단·H2 observed 격리·H3 soft-alias 물리key불변/items무변·H4 distill 멱등/TTL/캡) 라이브 실증, 웹 6화면 Playwright(XSS 5종 차단·콘솔에러0), 36툴·parity 65·전체 테스트·무회귀·합성 leftover 0. 발견 전부 low(진행로그 narrative 흠뿐).
- **P-V3-7(UserPromptSubmit recall + pgvector) = 후속 보류(윤상민 판단 대기)**: 시맨틱 recall 은 **pgvector 바이너리 부재로 차단**(ClickUp 백로그 86exz9688). pg_trgm(P3a)으로 ILIKE 가속은 적용됨. 세션시작 인덱스가 recalled K/D/F/H 를 이미 주입하므로 per-prompt ILIKE recall 은 점증가치·노이즈 위험이라 보류. pgvector 설치 후 시맨틱 recall 이 본 가치.

## item 폐기·ku 단일표면 흡수 (2026-06-19, MCP 36→34)

윤상민 결정: item/item_* 테이블 + search_items/get_item MCP 폐기, ku(knowledge_unit)로 완전 흡수. **라이브 item 물리 드랍 미실행**(스크립트만 — 윤상민 라이브 작업 중).

- **MCP 표면 36→34**: `search_items`·`get_item` 제거(`src/capabilities/items.ts` 빈 배열, `src/tools/items.ts` 삭제, `server.ts` registerItemTools 제거, `RESERVED_TOOL_NAMES`·`meaning.ts` 정리). REST `/api/ui/items`·`/api/ui/items/:id` 동반 제거 — **웹 app.js 미사용 확인(영향 0)**. parity 34툴 동결·전부 통과(63건).
- **활동필터 흡수(ctx_grep/ctx_ls)**: `system`(=external_system)·`since`(=occurred_at>=)·`source`(observed/authored)·`type`(원 item.type) 파라미터 추가 — `listKnowledge`/`searchKnowledge`(buildFilters)에 절 추가 + `ctx.ts` 어댑터(zod input + REST parse) 노출. type 무손실 복원 위해 `mirrorKnowledgeFromRawItem` 이 `fields._item_type` 가산 기록(redact 무관 enum).
- **thread 보존(ctx_cat)**: `ctx_cat { thread:true }` 가 `knowledgeThread`(parent_name 자기참조 WITH RECURSIVE, depth 20)로 ancestors/children 재구성 — 구 get_item.thread 동등. 기본 생략(토큰 절약). 라이브: notion 미러 40/41 parent_name 보유라 무손실.
- **가산이관 검증(item_domain/item_project→kud/kup)**: 라이브 **이미 100% 동기**(듀얼라이트 즉시반영) — `scripts/migrate-item-mappings-to-ku.mjs` 멱등 upsert **--apply 실행, PRE=POST 통과**(kud 299→299·kup 70→70 불변, 미흡수 0, ku-네이티브 llm 161행 무손상). 비파괴·재실행 안전.
- **드랍 스크립트(미실행)**: `scripts/drop-item-legacy.mjs` — 3중 잠금(--execute + ITEM_DROP_CONFIRM=YES + PRE 불변식 미러/흡수 누락 0) + pg_dump 백업 + FK 역순 드랍(item_legacy VIEW→relation→item_domain/project→item_mapping_audit→item). DRY 실행으로 PRE 불변식 통과 확인(item=71·누락 0). connector_state/person*/pm_write_audit 보존.
- **data_source 정정**: slack/discord `into_kinds` 를 실제(미적재 `[]`·status='dropped')로 — KIND_MAP 에 message:slack/message:discord 미정의라 ku 적재 0건인 현실과 정합(기존 `["K"]`/active 는 거짓 표기였음).
- **잔여(후속)**: (a) **듀얼라이트 제거 미적용** — ingestItems 의 item INSERT 제거는 재기동 전까지 구 코드가 item 에 계속 써서 컷오버 조율 필요(현 제약: 커밋/재기동 금지). (b) **curate_item_mapping ku-좌표 재배선 미적용** — 입력이 아직 정수 itemId(name 으로 교체는 파괴적 API 변경 → 라이브 세션 습관/런북 컷오버 조율 필요). 매핑은 이미 kud/kup 에 가산 동기됨. (c) item_mapping_audit(285행) 이력 이월·person actor 귀속 ku 보존·relation 평면 ku 이식 = 윤상민 확인 대기.
